name: Run Tests

on:
  push:
    branches:
     ["master"]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run :echo "Running tests"
      - run :echo "Tests passed