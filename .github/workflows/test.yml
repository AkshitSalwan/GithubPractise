name: Run Tests

on:
  push:
    branches:
     ["master"]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - run : node --version
      - run : npm --version
      - uses : actions/checkout@v3
      - run : "node index.js"